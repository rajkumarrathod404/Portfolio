import { Box } from "@mui/material";
import MainPage from "./Mainpage";

const Root = () => {
  return (
    <Box height={"90vh"} width={"100%"}>
      <MainPage />
    </Box>
  );
};

export default Root;
